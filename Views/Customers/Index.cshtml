@model Excercise.Models.Customer
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2>Customers</h2>
@if (true) { 
    @*{
            <p>We Don't Have Customers!</p>
        }

        else*@

    <table id="customers" class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>Customer</th>
            </tr>
        </thead>
        @*foreach (var customer in Model)
        {
            }*@
            <tbody>
                <tr>
                    <td>@Model.Name</td>
                </tr>
            </tbody>
        
    </table>

}