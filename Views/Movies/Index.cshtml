@model Excercise.Models.Movie
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Movies</h2>

@if (true)
{
    @*{
            <p>We Don't Have Customers!</p>
        }

        else*@

    <table id="customers" class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>Movies</th>
            </tr>
        </thead>
        @*foreach (var customer in Model)
            {
                }*@
        <tbody>
            <tr>
                <td>@Model.Name</td>
            </tr>
        </tbody>

    </table>

}

